// sets the values of an array called Node[n] to the values of the nodes in a path
// at Node[0] is the goal node
// at Node[NodeCount] is the start node
// note that Node[NodeCount] is the start position so the first node on the path should
// be Node[NodeCount - 1]
NodesVisited = 0;
var done = 0;
Node[0] = GoalCell;
NodeCount = 0;
while done = 0
{
    var temp = Node[NodeCount];
    if Calculated_List[temp,2] !=noone
    {
    NodeCount +=1;
    Node[NodeCount] = Calculated_List[temp,2];
    
    }
    else
    {done = 1;}
}
